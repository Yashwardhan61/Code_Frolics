{
  "name": "legacy-trunk-root",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "cd frontend && npm install && npm run build",
    "preview": "npm run preview --prefix frontend"
  }
}
